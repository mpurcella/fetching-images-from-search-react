import React from 'react';
import './ImageSearchHeading.scss';

const ImageSearchHeading = (props) => {
	return <h1 className='image-search-heading'>{props.heading}</h1>;
};

export default ImageSearchHeading;
